{
  "name": "aist",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:blog": "pnpm --filter my-real-hard-blog dev",
    "build": "node scripts/build.mjs",
    "build:vercel:blog": "pnpm --filter @aist/adapter-vercel... run build && pnpm --filter my-real-hard-blog build:vercel",
    "test": "pnpm -r test",
    "create": "pnpm exec create-aist-app"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "create-aist-app": "workspace:*",
    "pnpm": "^9.0.0",
    "typescript": "^5.0.0"
  }
}
